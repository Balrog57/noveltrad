import{_ as t,o,c as r,a2 as s}from"./chunks/framework.DpRDjl8b.js";const g=JSON.parse('{"title":"Gap Analysis — NovelTrad 2.1.3 → SDD 2.0","description":"","frontmatter":{},"headers":[],"relativePath":"audit/GAP_ANALYSIS_2.1.3_to_SDD.md","filePath":"audit/GAP_ANALYSIS_2.1.3_to_SDD.md","lastUpdated":1784326073000}'),n={name:"audit/GAP_ANALYSIS_2.1.3_to_SDD.md"};function d(i,e,a,c,l,u){return o(),r("div",null,[...e[0]||(e[0]=[s(`<h1 id="gap-analysis-—-noveltrad-2-1-3-→-sdd-2-0" tabindex="-1">Gap Analysis — NovelTrad 2.1.3 → SDD 2.0 <a class="header-anchor" href="#gap-analysis-—-noveltrad-2-1-3-→-sdd-2-0" aria-label="Permalink to &quot;Gap Analysis — NovelTrad 2.1.3 → SDD 2.0&quot;">​</a></h1><blockquote><p><strong>Date</strong> : 2026-07-05 <strong>Périmètre</strong> : Audit chirurgical du code en production (v2.1.3) vs le SDD VitePress (<code>docs/volumes/</code>). <strong>Cadrage</strong> : Durcissement de l&#39;existant — <strong>ne pas réinventer la roue</strong>. Le code 2.1.3 implémente déjà la stack cible (Electron 31 + Vue 3 + TS + SQLite + 10 agents Ollama). Le <code>WORKFLOW_STATE.md</code> affirme que tous les volumes sont couverts ; l&#39;audit révèle que la couverture est <strong>large mais inégale</strong> : plusieurs briques sont des <strong>bouchons heuristiques MVP</strong> non conformes au SDD, et le branching adaptatif (retry, QA score) est documenté mais absent. <strong>Stack actuelle confirmée</strong> : Electron 31 ESM · Vue 3.4 · TS 5.5 · <code>node-sqlite3-wasm</code> (synchrone, type better-sqlite3) · Ollama (<code>net.fetch</code>) · 782 tests verts · 10 agents · 10 prompts TS.</p></blockquote><hr><h2 id="🔁-revue-post-implementation-t1-t15-2026-07-06" tabindex="-1">🔁 Revue post-implémentation T1-T15 (2026-07-06) <a class="header-anchor" href="#🔁-revue-post-implementation-t1-t15-2026-07-06" aria-label="Permalink to &quot;🔁 Revue post-implémentation T1-T15 (2026-07-06)&quot;">​</a></h2><p>Suite à l&#39;audit initial, 15 tâches correctives (T1-T15) ont été implémentées. Une <strong>revue indépendante sceptique</strong> (lecture du code réel, pas des commits/messages) a été conduite. Verdict factuel :</p><table tabindex="0"><thead><tr><th>#</th><th>Tâche</th><th>Verdict</th><th>Preuve file:line</th></tr></thead><tbody><tr><td><strong>T1</strong></td><td>Sécurité (CSP/safeStorage/preload)</td><td>✅ <strong>Conforme</strong></td><td>CSP prod active <code>index.ts:55-89</code> + <code>session.defaultSession.webRequest.onHeadersReceived:81-88</code> ; <code>safeStorage</code> <code>secrets.ts:36-85</code>, SALT hardcodé supprimé ; preload log gardé par <code>import.meta.env.DEV</code> <code>preload/index.ts:102-103</code></td></tr><tr><td><strong>T2</strong></td><td>Migration runner unifié</td><td>✅ <strong>Conforme</strong></td><td>Runner file-based <code>connection.ts:67-94</code>, <code>009_chapter_metadata.sql</code> existe, inline array supprimé. ⚠️ transaction raw <code>BEGIN</code> fragile (<code>connection.ts:81-82</code>) → Phase 4B</td></tr><tr><td><strong>T3</strong></td><td>Workflow (retry/branching/queue/resume)</td><td>⚠️ <strong>Partiel</strong> 🐛</td><td>Retry+branching QA <code>WorkflowEngine.ts:514-555</code> + p-queue <code>:772</code> + auto-resume <code>index.ts:270-272</code> présents. <strong>MAIS</strong> 🐛 double p-retry (AiRouter × Provider = jusqu&#39;à 16 tentatives <code>AiRouter.ts:83</code> + <code>OllamaProvider.ts:26</code>) ; 🐛 auto-resume ignore jobs <code>single</code> (<code>WorkflowEngine.ts:874</code>)</td></tr><tr><td><strong>T4</strong></td><td>Câbler 3 agents LLM</td><td>✅ <strong>Conforme</strong></td><td>Consistency/Lexicon/Qa appellent <code>aiRouter.chat()</code> avec leurs prompts importés + fallback heuristique (<code>ConsistencyAgent.ts:57-64</code>, <code>LexiconAgent.ts:47-54</code>, <code>QaAgent.ts:72-79</code>)</td></tr><tr><td><strong>T5</strong></td><td>PromptLoader DB</td><td>⚠️ <strong>Partiel</strong> 🐛</td><td><strong>Dead code</strong> : class correcte mais jamais instanciée, <code>setPromptLoader</code> jamais appelée, queries colonne <code>active</code> inexistante (table <code>prompts</code> migration 005 n&#39;a pas cette colonne) → override DB non fonctionnel</td></tr><tr><td><strong>T6</strong></td><td>Agent I/O Zod</td><td>✅ <strong>Conforme</strong></td><td><code>inputSchema</code>/<code>outputSchema</code> <code>Agent.ts:24,27</code>, <code>validateOutput()</code> appelé <code>WorkflowEngine.ts:501-512</code>, 10 agents équipés, 73 tests</td></tr><tr><td><strong>T7</strong></td><td>ConsistencyChecker 7/7</td><td>✅ <strong>Conforme</strong></td><td>7 métriques <code>ConsistencyChecker.ts:145-293</code>, pondération SDD §11.5 exacte <code>:82-90</code>, caps <code>:305-314</code>, tolérances zh-fr = SDD <code>:17-24</code></td></tr><tr><td><strong>T8</strong></td><td>HallucinationDetector</td><td>⚠️ <strong>Partiel</strong> 🐛</td><td>Détecteur appelé <strong>MAIS</strong> hallucination = hardcoded <code>95</code> en fallback (<code>QualityChecker.ts:43-45</code>) ; QaAgent fallback ne passe pas <code>consistencyReport</code> (<code>QaAgent.ts:58-63</code>) → dimension = 90</td></tr><tr><td><strong>T9</strong></td><td>EPUB epub-gen-memory</td><td>⚠️ <strong>Partiel</strong> 🐛</td><td>Dep <code>epub-gen-memory@^1.1.2</code> + import <code>ExportEngine.ts:14</code> + utilisé single <code>:565</code> et multi <code>:284</code>. <strong>MAIS</strong> 🐛 multi-chapitre hardcode <code>lang:&quot;fr&quot;</code> (<code>ExportEngine.ts:279</code>) ignore targetLanguage</td></tr><tr><td><strong>T10</strong></td><td>EPUB/DOCX import</td><td>✅ <strong>Conforme</strong></td><td><code>readEpubSpine()</code> <code>ProjectManager.ts:858-899</code> lit le spine OPF ; DOCX mammoth styleMap Heading 1 <code>:752-766</code></td></tr><tr><td><strong>T11</strong></td><td>TM 5 tiers</td><td>⚠️ <strong>Partiel</strong> 🐛</td><td>Segmentation + exact + global réels <strong>MAIS</strong> <code>findBestMatch()</code> 5-tier cascade a <strong>0 caller production</strong> (TranslateAgent n&#39;appelle que <code>fuzzyMatches</code>) ; tier 5 = null pas embeddings</td></tr><tr><td><strong>T12</strong></td><td>TM MiniSearch</td><td>✅ <strong>Conforme</strong> ⚠️</td><td>Two-pass implémenté. 🐛 préfiltre SQL <code>\\w{3,}</code> Latin-only → CJK fuzzy dégrade vers fallback</td></tr><tr><td><strong>T13</strong></td><td>RAG batch/seuil/reindex</td><td>⚠️ <strong>Partiel</strong> 🐛</td><td>Seuil ✅ (0.7). <strong>MAIS</strong> <code>computeEmbeddings</code>/<code>storeEmbeddings</code> batch + <code>reindex()</code> = <strong>dead code jamais appelé</strong> ; sqlite-vec declared unused (POC KO)</td></tr><tr><td><strong>T14</strong></td><td>Worker threads</td><td>🐛 <strong>Bug critique</strong></td><td>Fix named-export correct <strong>MAIS</strong> path <code>../services/agents/\${agentId}.js</code> avec stage lowercase (<code>&quot;translate&quot;</code>) alors que fichiers sont PascalCase (<code>TranslateAgent.ts</code>) → <strong>chaque import worker échoue → fallback silencieux systématique</strong> (workers jamais fonctionnés)</td></tr><tr><td><strong>T15</strong></td><td>Signature code</td><td>🚫 <strong>WONTFIX</strong></td><td>Signature définitivement exclue (décision produit). Config CSC/Apple retirée de <code>electron-builder.yml</code> et <code>release.yml</code>. Builds non signés → Windows SmartScreen avertira au premier lancement.</td></tr></tbody></table><p><strong>Bilan revue</strong> : 6 conformes · 6 partielles (bugs) · 1 bug critique · 1 non-faite. <strong>Plusieurs features = dead code non câblé.</strong></p><h3 id="note-rag-decision-utilisateur-2026-07-06" tabindex="-1">Note RAG (décision utilisateur 2026-07-06) <a class="header-anchor" href="#note-rag-decision-utilisateur-2026-07-06" aria-label="Permalink to &quot;Note RAG (décision utilisateur 2026-07-06)&quot;">​</a></h3><p>Le SDD §9.3 (<code>docs/volumes/09-Translation-Memory.md:87-138</code>) <strong>ne requiert aucune lib vectorielle</strong> — brute-force + cosinus JS est 100% conforme (seuil <code>&gt; 0.75</code>, critères d&#39;acceptation qualitatifs uniquement). Le POC <code>sqlite-vec</code> a échoué sur <code>node-sqlite3-wasm</code> (pas de <code>loadExtension</code>). <strong>Décision</strong> : abandonner <code>sqlite-vec</code>, garder MiniSearch préfiltre + cosinus JS, optimiser (batch, cache, reindex réel). La dépendance <code>sqlite-vec@^0.1.9</code> sera supprimée.</p><h3 id="feuille-de-route-residuelle-cycles-correctifs-suivant" tabindex="-1">Feuille de route résiduelle (cycles correctifs suivant) <a class="header-anchor" href="#feuille-de-route-residuelle-cycles-correctifs-suivant" aria-label="Permalink to &quot;Feuille de route résiduelle (cycles correctifs suivant)&quot;">​</a></h3><ul><li>✅ <strong>Phase 1 (COMBLÉE 2026-07-06)</strong> — 3 bugs runtime : T3 double-retry (commit 68b3b19), T9 EPUB lang (38174ab), T14 worker path (a2bd1fa)</li><li>✅ <strong>Phase 2 (COMBLÉE 2026-07-06)</strong> — Wiring dead code : T5 PromptLoader câblé (dedf82b), T11 findBestMatch actif (7fb56ee), T13 RAG batch/reindex/cache + rm sqlite-vec (cb841a5)</li><li>✅ <strong>Phase 3 (COMBLÉE 2026-07-06)</strong> — Dégradations : T8 hallucination fallback 0 + consistencyReport transmis (3ab178b), T12 tokenization Unicode CJK (84a9809)</li><li>✅ <strong>Phase 4 (COMBLÉE 2026-07-06)</strong> — Cleanup : T3 single-job marqués failed (7eedf07), T2 robustesse transactions migrations (a263779)</li><li>🚫 <strong>WONTFIX</strong> : T15 signature code (décision produit définitive — la signature ne sera jamais activée)</li><li>✅ <strong>Bonus</strong> : T4A jobs single abandonnés marqués failed au redémarrage, T4B transactions imbriquées supportées</li></ul><p><strong>Bilan final</strong> : <strong>940 tests</strong> (62 suites), 0 failed, 0 type-check error. 13 commits atomiques. <strong>Tous les écarts identifiés par la revue indépendante sont comblés</strong>, à l&#39;exception de T15 (signature) qui est un WONTFIX produit assumé — les builds sont délibérément non signés.</p><hr><h2 id="tl-dr-—-verdict-par-bloc-critique" tabindex="-1">TL;DR — verdict par bloc critique <a class="header-anchor" href="#tl-dr-—-verdict-par-bloc-critique" aria-label="Permalink to &quot;TL;DR — verdict par bloc critique&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Bloc</th><th>Maturité réelle vs SDD</th><th>Verdict</th></tr></thead><tbody><tr><td>Architecture Electron (ESM, sandbox, preload CJS, IPC Zod)</td><td>✅ Solide</td><td>Conforme SDD 01/21 (à l&#39;exception CSP + signature)</td></tr><tr><td>Base de données SQLite</td><td>⚠️ Partiel</td><td>14/15 tables SDD, mais dual-runner migrations, table <code>history</code> renommée, <strong>aucune recherche vectorielle native</strong></td></tr><tr><td>Workflow / orchestration</td><td>⚠️ Partiel</td><td>Pipeline séquentiel fiable, mais <strong>pas de retry, pas de queue, pas de branching QA, pas d&#39;auto-resume</strong></td></tr><tr><td>10 Agents</td><td>⚠️ Partiel</td><td>Tous présents, mais <strong>9/10 prompts TS non câblés</strong> et 6 agents tournent en heuristique pure sans LLM</td></tr><tr><td>Prompts (Vol 25)</td><td>❌ Faible</td><td><code>prompts</code> table morte, pas de versioning, pas de frontmatter YAML, 9/10 non utilisés</td></tr><tr><td>Mémoire de traduction (Vol 09)</td><td>❌ Faible</td><td>Pas de segmentation phrase, pas de match exact câblé, pas de priorité 5 tiers, pas de global TM</td></tr><tr><td>RAG / embeddings</td><td>⚠️ Partiel</td><td>Fonctionne mais <strong>brute-force O(n) en JS</strong>, pas de seuil, pas de réindexation, 1 round-trip Ollama/paragraphe</td></tr><tr><td>Lexique (Vol 10)</td><td>⚠️ Partiel</td><td>Regex OK mais <strong>pas de préservation casse, pas de forbidden words, pas d&#39;import</strong></td></tr><tr><td>Consistency (Vol 11)</td><td>❌ Faible</td><td><strong>3 métriques sur 7 manquantes</strong> (dialogues/nombres/markup), formule de score fausse, tolérances trop laxes</td></tr><tr><td>Quality (Vol 12)</td><td>❌ Faible</td><td>Heuristique MVP, <strong>5/8 dimensions = constantes</strong>, pas de LLM, HallucinationDetector non câblé</td></tr><tr><td>Export (Vol 13)</td><td>⚠️ Partiel</td><td>5 formats (MD/TXT/HTML/DOCX/EPUB), mais <strong>EPUB maison sans spine/nav</strong>, pas de PDF, lib SDD non utilisée</td></tr><tr><td>Import / parsing (Vol 05)</td><td>⚠️ Partiel</td><td>mammoth+adm-zip+cheerio OK mais <strong>EPUB flatten sans spine</strong>, <strong>DOCX sans détection Heading 1</strong></td></tr><tr><td>Sécurité (Vol 21)</td><td>⚠️ Partiel</td><td>webPreferences locked, AES-256-GCM, nonce plugins — mais <strong>CSP désactivée en prod, pas de signature, sel KDF hardcodé, preload log les args IPC</strong></td></tr><tr><td>Auto-update (Vol 17)</td><td>✅ Solide</td><td>electron-updater câblé, canaux paramétrables (manque signature)</td></tr><tr><td>Plugins (Vol 15)</td><td>✅ Solide</td><td>PluginHost + contexte + hot-reload + exemple ESM — complet</td></tr></tbody></table><hr><h2 id="_1-cartographie-de-l-existant-vs-cible-2-0" tabindex="-1">1. Cartographie de l&#39;Existant vs Cible 2.0 <a class="header-anchor" href="#_1-cartographie-de-l-existant-vs-cible-2-0" aria-label="Permalink to &quot;1. Cartographie de l&#39;Existant vs Cible 2.0&quot;">​</a></h2><h3 id="_1-1-ce-qui-est-100-recuperable-ne-pas-toucher" tabindex="-1">1.1 Ce qui est 100 % récupérable (ne pas toucher) <a class="header-anchor" href="#_1-1-ce-qui-est-100-recuperable-ne-pas-toucher" aria-label="Permalink to &quot;1.1 Ce qui est 100 % récupérable (ne pas toucher)&quot;">​</a></h3><ul><li><strong>Stack Electron 31 ESM + electron-vite + electron-builder</strong> — configuration éprouvée, build Windows signable.</li><li><strong>Main process entry</strong> <code>apps/desktop/src/main/index.ts:160-185</code> — <code>webPreferences</code> strict (sandbox:true, contextIsolation:true, nodeIntegration:false, webSecurity:true), preload forcé en <strong>CJS</strong> (<code>electron.vite.config.ts:33-39</code>) pour contourner le bug Electron #41460 (sandbox ≠ ESM preload). <strong>Garder tel quel.</strong></li><li><strong>Preload</strong> <code>apps/desktop/src/preload/index.ts</code> — pont <code>contextBridge</code> avec allowlist de 79 canaux.</li><li><strong>Couche IPC</strong> — 66 handlers Zod-validés (<code>apps/desktop/src/main/ipc/handlers/*.ts</code>), router modulaire avec fault isolation par domaine.</li><li><strong>Driver SQLite</strong> — <code>node-sqlite3-wasm</code> (<code>apps/desktop/src/main/db/connection.ts</code>) utilisé <strong>synchrone</strong> comme better-sqlite3 (WAL + FK ON). <strong>Garder, ne pas migrer vers better-sqlite3</strong> (cf. §2.1).</li><li><strong>OllamaProvider / OllamaManager</strong> — utilisation correcte d&#39;<code>electron.net.fetch</code> (v2.1.3), streaming NDJSON, <code>AbortSignal.timeout()</code>. Bonne implémentation.</li><li><strong>StructuredLogger</strong> (<code>utils/logger.ts</code>) — NDJSON, redaction, correlation IDs, <code>child()</code>. Conforme SDD 18.</li><li><strong>Crypto secrets</strong> (<code>utils/secrets.ts</code>) — AES-256-GCM, format <code>base64(iv+authTag+ciphertext)</code>. Algo correct, dérivation à durcir.</li><li><strong>CalibrationService</strong> (<code>services/CalibrationService.ts</code>) — régression linéaire least-squares par dimension, formule SDD §12.5 exacte. Bien bâti.</li><li><strong>PluginHost / PluginContext / Disposable</strong> (Vol 15) — complet, hot-reload dev, exemple ESM pré-compilé. Conserve.</li><li><strong>Système UI</strong> — composants <code>Nt*</code> + vues + stores Pinia + router. Mature, ne pas toucher.</li><li><strong>AI cache</strong> (<code>services/AiCache.ts</code>) — hash SHA-256(system+user+model+temp). Bon.</li><li><strong>805 tests</strong> (782 unit + E2E) — filet de sécurité solide pour refactor.</li></ul><h3 id="_1-2-ce-qui-doit-evoluer-reecriture-partielle" tabindex="-1">1.2 Ce qui doit évoluer (réécriture partielle) <a class="header-anchor" href="#_1-2-ce-qui-doit-evoluer-reecriture-partielle" aria-label="Permalink to &quot;1.2 Ce qui doit évoluer (réécriture partielle)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Bloc</th><th>Fichier(s)</th><th>Problème</th><th>Action</th></tr></thead><tbody><tr><td>Migrations DB</td><td><code>db/connection.ts:8-362</code> + <code>db/migrations/*.sql</code></td><td><strong>Dual-runner</strong> : tableau inline v1-9 ombre les 8 fichiers <code>.sql</code> (jamais exécutés). v9 inline n&#39;a pas de <code>.sql</code> jumeau.</td><td>Source unique = fichiers <code>.sql</code>. Créer <code>009_chapter_metadata.sql</code>. Supprimer le tableau inline.</td></tr><tr><td><code>history</code> table</td><td><code>002_jobs.sql:42</code> vs SDD §6.2</td><td>SDD nomme <code>history</code>, code crée <code>history_snapshots</code> avec un schéma différent (JSON <code>paragraphs</code>/<code>metadata</code>).</td><td>Soit renommer + aligner, soit <strong>mettre à jour le SDD §6.2</strong> pour entériner le design <code>history_snapshots</code> (recommandé : le design code est plus riche).</td></tr><tr><td><code>statistics</code> table</td><td><code>005_alias_export_prompts_stats.sql:37</code> vs SDD §6.2</td><td>SDD = agrégat 1 ligne/projet (<code>total_chapters</code>...) ; code = table long/thin (<code>metric</code>, <code>value</code>). Incompatibles.</td><td>Soit implémenter la vue agrégée SDD, soit fixer le SDD sur le modèle long/thin (plus flexible).</td></tr><tr><td>WorkflowEngine</td><td><code>managers/WorkflowEngine.ts</code></td><td>Ordonnanceur séquentiel, <code>maxConcurrentJobs</code> lu <strong>mais jamais appliqué</strong>, pas de retry, pas de branching QA.</td><td>Introduire <code>p-queue</code> + <code>p-retry</code> + branch score (cf. §2.3 + §3).</td></tr><tr><td><code>jobs</code> table</td><td><code>002_jobs.sql:10</code> + <code>JobRepository.ts</code></td><td><strong>Ledger de statut, pas une queue</strong> (pas de <code>claimed_by</code>, <code>next_run_at</code>, dequeue atomique).</td><td>Étendre le schéma + ajouter dequeue atomique OU adopter p-queue en mémoire + persistance minimale.</td></tr><tr><td>Agents heuristic-only</td><td><code>services/agents/{Consistency,Lexicon,Grammar,Style,Polish,Qa}Agent.ts</code></td><td>6 agents ne font <strong>aucun appel LLM</strong>, tournent en heuristique locale, ignorent leur system prompt SDD.</td><td>Câbler les prompts TS via <code>AiRouter.chat()</code> (les prompts existent déjà, ils sont juste morts).</td></tr><tr><td>ConsistencyChecker</td><td><code>services/ConsistencyChecker.ts</code></td><td>3/7 métriques manquantes (dialogues/nombres/markup), formule score fausse, tolérances trop laxes.</td><td>Compléter les 7 métriques + appliquer pondération SDD §11.5 + caps.</td></tr><tr><td>QualityChecker</td><td><code>services/QualityChecker.ts:9-44</code></td><td>MVP heuristique, 5/8 dimensions = constantes, <code>comments = &quot;Scoring heuristique MVP&quot;</code>.</td><td>Brancher <code>ConsistencyReport.globalScore</code>, <code>HallucinationDetector</code>, et LLM eval pour fluency/style/dialogue.</td></tr><tr><td>LexiconEngine.apply</td><td><code>services/LexiconEngine.ts:25-50</code></td><td>Regex replace sans préservation de casse, pas de contrôle <code>forbidden</code>, pas de <code>import()</code>.</td><td>Ajouter préservation casse (regex capture + transform), check forbidden → warning, méthode <code>import()</code>.</td></tr><tr><td>RagEngine.findSimilar</td><td><code>services/RagEngine.ts:81-125</code></td><td>Brute-force O(n) sur tous les embeddings projet, pas de seuil, pas de batching.</td><td>Soit <code>sqlite-vec</code> KNN, soit MiniSearch préfiltre + Levenshtein. (cf. §2.6)</td></tr><tr><td>ExportEngine.toEpub</td><td><code>services/ExportEngine.ts:605-641</code> (+ <code>:240-351</code>)</td><td>EPUB maison sans spine/TOC/nav.xhtml/NCX, langue hardcodée <code>fr</code>.</td><td>Migrer vers <code>epub-gen-memory</code> (cf. §2.4).</td></tr><tr><td>ProjectManager.extractEpub/Docx</td><td><code>managers/ProjectManager.ts:761-881</code></td><td>EPUB flatten sans spine ; DOCX perd les Heading 1 → chapter split foireux.</td><td>EPUB : lire <code>content.opf</code> spine ; DOCX : <code>mammoth</code> styleMap Heading 1.</td></tr></tbody></table><h3 id="_1-3-ce-qui-est-manquant-a-batir" tabindex="-1">1.3 Ce qui est manquant (à bâtir) <a class="header-anchor" href="#_1-3-ce-qui-est-manquant-a-batir" aria-label="Permalink to &quot;1.3 Ce qui est manquant (à bâtir)&quot;">​</a></h3><ul><li><strong>Branche adaptative QA</strong> (SDD §7.1) : <code>score≥90 → export</code>, <code>70-89 → retry weakest step</code>, <code>&lt;70 → pause</code>. <strong>Totalement absent</strong> — <code>qualityThreshold</code> accepté en IPC mais jamais consommé (<code>WorkflowEngine.ts</code>).</li><li><strong>Retry réseau Ollama</strong> (SDD §7.10) : <code>Retry ×3 + backoff exponentiel + fallback provider</code>. Aucun retry dans <code>OllamaProvider.chat()</code> ni <code>AiRouter</code>.</li><li><strong>Auto-resume des jobs au démarrage</strong> (SDD §7.11) : <code>app.whenReady()</code> (<code>index.ts:207-287</code>) <strong>n&#39;appelle jamais</strong> <code>JobRepository.listActive()</code>. Les jobs <code>running</code>/<code>paused</code> restent bloqués après un crash ; seuls les batch peuvent être repris manuellement.</li><li><strong>Segmentation phrase de la TM</strong> (SDD §9.2 <code>segmentSentences</code>) : nulle part. TM travaille au niveau paragraphe uniquement.</li><li><strong>Match exact TM câblé</strong> (SDD §9.9) : <code>exactMatch()</code> existe mais n&#39;est jamais appelé par aucun agent.</li><li><strong>TM globale cross-projet</strong> + <strong>priorité 5 tiers</strong> (SDD §9.4) : absent.</li><li><strong>Versioning des prompts en DB</strong> (SDD §25.11) : <code>prompts</code> table existe mais n&#39;est jamais lue/écrite. Pas de <code>frontmatter</code> YAML, pas de résolution &quot;latest version&quot;.</li><li><strong>CSP production</strong> (SDD §21.6) : <code>setupCspHeaders()</code> (<code>index.ts:54-91</code>) early-return quand <code>!VITE_DEV_SERVER_URL</code>. <strong>Pas de CSP en production</strong>, pas de meta tag dans <code>index.html</code>.</li><li><strong>Signature de code</strong> (SDD §21.9) : <code>electron-builder.yml</code> à <code>forceCodeSigning:false</code>, <code>verifyUpdateCodeSignature:false</code>, <code>signAndEditExecutable:false</code>. Pas d&#39;Authenticode.</li><li><strong>OS keyring</strong> (SDD §21.5) : <code>keytar</code>/<code>safeStorage</code> mentionnés en commentaire <code>secrets.ts:5,9-11</code> mais <strong>jamais importés</strong>. KDF via <code>scryptSync(userData, SALT=&quot;NovelTrad-v1-key-derivation&quot;, 32)</code> — sel <strong>hardcodé constant</strong>.</li><li><strong>PDF export</strong> (SDD §13.7 futur) : pas de cas <code>pdf</code> dans <code>ExportEngine.render()</code>, pas de plugin PDF livré.</li><li><strong>Agent I/O JSON Schema validation</strong> (SDD §8.13) : l&#39;interface <code>Agent</code> (<code>agents/Agent.ts:7-14</code>) n&#39;a <strong>aucun champ</strong> <code>inputSchema</code>/<code>outputSchema</code>. Pas de <code>validateOutput()</code> dans le runner.</li><li><strong>Réindexation embeddings manuelle</strong> (SDD §9) : pas de méthode <code>index()</code> / rebuild.</li></ul><hr><h2 id="_2-analyse-des-ecarts-par-volume-critique-recommandation-oss" tabindex="-1">2. Analyse des Écarts par Volume Critique + recommandation OSS <a class="header-anchor" href="#_2-analyse-des-ecarts-par-volume-critique-recommandation-oss" aria-label="Permalink to &quot;2. Analyse des Écarts par Volume Critique + recommandation OSS&quot;">​</a></h2><h3 id="_2-1-base-de-donnees-vol-06" tabindex="-1">2.1 Base de données (Vol 06) <a class="header-anchor" href="#_2-1-base-de-donnees-vol-06" aria-label="Permalink to &quot;2.1 Base de données (Vol 06)&quot;">​</a></h3><p><strong>Écart technique</strong></p><ul><li>Driver : <code>node-sqlite3-wasm</code> au lieu de <code>better-sqlite3</code>. API synchrone équivalente, mais ce n&#39;est pas la lib nommée par le SDD.</li><li><strong>Dual migration runner</strong> (<code>connection.ts:8-429</code>) : un tableau inline <code>MIGRATIONS</code> (v1-9) + un lecteur de fichiers <code>.sql</code> (v1-8). Les fichiers ne s&#39;exécutent jamais sur DB fraîche car le tableau inline enregistre déjà v1-8 dans <code>__migrations</code>. v9 inline n&#39;a pas de <code>.sql</code>.</li><li><code>history</code> → <code>history_snapshots</code> (renommage + redesign JSON non documenté au SDD).</li><li><code>statistics</code> : shape long/thin ≠ agrégat SDD.</li><li><strong>Aucune extension vectorielle</strong> chargée. <code>embeddings.embedding_json TEXT</code> = JSON sérialisé, pas de colonne native, pas d&#39;index KNN, pas de FTS5/BM25.</li><li>Pas de <code>down</code> migration (SDD §6.4 non implémenté).</li></ul><p><strong>Recommandation OSS</strong></p><ul><li>❌ <strong>Ne pas migrer vers <code>better-sqlite3</code></strong> — <code>electron-rebuild</code> + toolchain C++ Windows + friction <code>app.asar.unpacked</code> à chaque montée de version Electron. <strong><code>node-sqlite3-wasm</code> est le bon choix</strong> (WASM, synchrone, zero rebuild, perf suffisante pour mono-utilisateur desktop). <strong>Mettre à jour le SDD §6.1</strong> pour entériner.</li><li>✅ <strong><code>sqlite-vec</code></strong> pour la recherche vectorielle — extension C précompilée, API <code>CREATE VIRTUAL TABLE v USING vec0(emb float[768])</code> + <code>WHERE emb MATCH ? ORDER BY distance LIMIT k</code>. ⚠️ Pour <code>node-sqlite3-wasm</code>, vérifier le binding <code>sqlite-vec-wasm</code> ou charger le <code>.wasm</code> via le bridge WASM (les bindings officiels visent better-sqlite3 et node:sqlite). <strong>Prouver le POC avant d&#39;adopter.</strong><ul><li><code>npm i sqlite-vec</code> (ou variant wasm).</li></ul></li><li><strong>Action migrations</strong> : source unique = fichiers <code>.sql</code> triés numériquement, créer <code>009_chapter_metadata.sql</code>, supprimer le tableau inline <code>MIGRATIONS</code>.</li></ul><h3 id="_2-2-workflow-orchestration-vol-07" tabindex="-1">2.2 Workflow / orchestration (Vol 07) <a class="header-anchor" href="#_2-2-workflow-orchestration-vol-07" aria-label="Permalink to &quot;2.2 Workflow / orchestration (Vol 07)&quot;">​</a></h3><p><strong>Écart technique</strong></p><ul><li>Pipeline = <code>for await</code> séquentiel dans <code>WorkflowRunner.runFromIndex()</code> (<code>WorkflowEngine.ts:376-420</code>). Pas de DAG, pas de parallélisme intra-chapitre ni intra-stage.</li><li><code>maxConcurrentJobs</code> lu (<code>WorkflowEngine.ts:694</code>) mais <strong>jamais vérifié</strong> avant de lancer un runner → <code>start()</code>/<code>startBatch()</code> créent un runner inconditionnellement.</li><li><strong>Pas de retry</strong> malgré SDD §7.10. <code>OllamaProvider.chat()</code> (<code>OllamaProvider.ts:23-42</code>) : un seul <code>net.fetch</code>, 300 s timeout, throw immédiat sur HTTP error.</li><li><strong>Pas de branching QA</strong> malgré SDD §7.1. <code>QaAgent</code> calcule le score, <code>WorkflowRunner</code> l&#39;ignore. <code>qualityThreshold</code> présent en schéma IPC mais non consommé.</li><li><strong>Pas d&#39;auto-resume au démarrage</strong> malgré SDD §7.11. <code>index.ts:207-287</code> n&#39;appelle jamais <code>listActive()</code>. Resume batch = manuel via renderer uniquement.</li><li>Worker threads (<code>agent-worker.ts</code>) : importe les agents par chemin et appelle <code>module.default</code> — or les agents exportent des <strong>classes nommées</strong>, pas default. Le worker échoue silencieusement et retombe sur <code>agent.execute()</code> direct → fonctionnalité morte.</li></ul><p><strong>Recommandation OSS</strong></p><ul><li>❌ <strong><code>bullmq</code> + Redis</strong> — surdimensionné pour une app desktop mono-utilisateur. Rejeter.</li><li>✅ <strong><code>p-queue</code></strong> — file asynchrone pure JS, <code>new PQueue({concurrency})</code>, <code>.add(fn,{priority})</code>. À utiliser pour : (a) <code>maxConcurrentJobs</code> gate au niveau <code>WorkflowEngine.start()</code>, (b) parallélisme optionnel intra-batch (chapitres indépendants). <ul><li><code>npm i p-queue</code></li></ul></li><li>✅ <strong><code>p-retry</code></strong> — wrap <code>OllamaProvider.chat()</code> / <code>AiRouter.chat()</code> avec retry ×3 + backoff exponentiel (<code>ExponentialBackoff</code>). Implémente directement SDD §7.10. <ul><li><code>npm i p-retry</code></li></ul></li><li><strong>Crash recovery</strong> : étendre la table <code>jobs</code> avec <code>attempts INTEGER</code>, <code>next_run_at TEXT</code>, <code>claimed_by TEXT</code> + fonction <code>claimNextJob()</code> atomique (<code>UPDATE ... WHERE status=&#39;pending&#39; RETURNING *</code>). Une centaine de lignes max, plus léger que toute lib durable.</li><li><strong>Branching QA</strong> : dans <code>WorkflowRunner.runStep(&#39;qa&#39;)</code>, après <code>agent.execute()</code>, lire <code>result.score</code> → si <code>≥qualityThreshold</code> continuer, si <code>70-89</code> retry weakest step, si <code>&lt;70</code> <code>pause()</code>. ~30 lignes.</li></ul><h3 id="_2-3-agents-prompts-vol-08-25" tabindex="-1">2.3 Agents + prompts (Vol 08 + 25) <a class="header-anchor" href="#_2-3-agents-prompts-vol-08-25" aria-label="Permalink to &quot;2.3 Agents + prompts (Vol 08 + 25)&quot;">​</a></h3><p><strong>Écart technique</strong> — le plus gros trou fonctionnel du projet :</p><ul><li><strong>10 agents présents</strong>, mais <strong>6 sont purement heuristiques</strong> et <strong>n&#39;appellent jamais l&#39;LLM</strong> : <code>ConsistencyAgent</code>, <code>LexiconAgent</code>, <code>GrammarAgent</code>, <code>StyleAgent</code>, <code>PolishAgent</code>, <code>QaAgent</code>. (<code>SplitAgent</code> et <code>ExportAgent</code> légitimement sans LLM.)</li><li><strong>9 system prompts sur 10 sont importés nulle part</strong> — seul <code>TranslateAgent</code> importe et utilise <code>TRANSLATE_SYSTEM_PROMPT</code>. Les 9 autres <code>*_SYSTEM_PROMPT</code> sont du code mort.</li><li><code>prompts</code> table (Vol 25) : <strong>jamais lue ni écrite</strong>. Pas de versioning, pas de résolution latest, pas de frontmatter YAML <code>target_model</code>/<code>version</code>.</li><li>Pas de JSON-Schema I/O par agent (SDD §8.13).</li><li><code>LexiconAgent</code> appelle directement <code>lexiconEngine.apply()</code> (regex) en court-circuitant le prompt LLM SDD §25 Agent-4.</li></ul><p><strong>Recommandation</strong></p><ul><li><strong>Câbler les prompts existants</strong> dans les 6 agents concernés via <code>this.services.aiRouter.chat(SYSTEM_PROMPT, userPrompt, {jsonMode:true})</code>. Les fichiers prompts <strong>existent déjà</strong>, il suffit de les importer et de construire le <code>build*UserPrompt()</code>. C&#39;est un travail de câblage, pas de rédaction.</li><li>Implémenter un <strong><code>PromptLoader</code></strong> qui lît la table <code>prompts</code> (override runtime) avec fallback sur les constantes TS (défaut compilé). Résolution &quot;latest version&quot; par <code>ORDER BY version DESC LIMIT 1</code>. Permet à un utilisateur avancé de tuner un prompt sans rebuild.</li><li>Ajouter <code>inputSchema</code>/<code>outputSchema</code> (Zod) à l&#39;interface <code>Agent</code> + <code>validateOutput()</code> dans le runner.</li></ul><h3 id="_2-4-export-import-vol-13-05" tabindex="-1">2.4 Export / Import (Vol 13 + 05) <a class="header-anchor" href="#_2-4-export-import-vol-13-05" aria-label="Permalink to &quot;2.4 Export / Import (Vol 13 + 05)&quot;">​</a></h3><p><strong>Écart technique — Export</strong></p><ul><li>5 formats : <code>markdown</code>, <code>txt</code>, <code>html</code>, <code>docx</code>, <code>epub</code> (<code>ExportEngine.render()</code> switch <code>:491-504</code>). <strong>Pas de PDF.</strong></li><li><strong>EPUB maison via <code>adm-zip</code></strong> — pas la lib <code>epub-gen-memory</code> du SDD §13.3. Lacunes : pas de <code>nav.xhtml</code>, pas de NCX, langue hardcodée <code>fr</code> (<code>:337</code>), OPF single-chapter sans <code>properties=&quot;nav&quot;</code> (<code>:631-636</code>). Multi-chapitre meilleur (<code>:240-351</code>) mais reste artisanal.</li><li><code>CustomRenderer</code> Map + <code>registerRenderer()</code> existent pour plugins → bon point d&#39;extension.</li></ul><p><strong>Écart technique — Import</strong></p><ul><li>EPUB : <code>adm-zip</code> + <code>cheerio</code> (<code>ProjectManager.ts:761-806</code>) itère les entrées HTML <strong>sans lire le spine/TOC</strong> de <code>content.opf</code>. Concatène tout puis re-split par regex → structure de chapitres perdue.</li><li>DOCX : <code>mammoth.convertToHtml</code> puis regex HTML→Markdown (<code>:821-881</code>). <strong>Les Heading 1 sont aplatis en <code>#</code></strong> au lieu d&#39;être utilisés comme délimiteurs de chapitres (SDD §5.5).</li><li>TXT/MD : <code>chardet.detectFileSync</code> + <code>iconv-lite.decode</code> — correct.</li><li>Détection langue : <code>franc</code> (<code>:887-919</code>) — correct.</li></ul><p><strong>Recommandation OSS</strong></p><ul><li>✅ <strong><code>epub-gen-memory</code></strong> — remplace le EPUB maison. Maintenu, multi-chapitre natif, <code>lang</code>, <code>css</code> custom, EJS templates, EPUB v2/v3. Gère spine/nav/NCX correctement. ⚠️ Images fetchées par URL → passer data-URI pour local-first. <ul><li><code>npm i epub-gen-memory</code></li></ul></li><li>✅ <strong><code>mammoth</code> conservé</strong>, mais ajouter un <code>styleMap</code> : <code>&quot;p[style-name=&#39;Heading 1&#39;] =&gt; chapter:fresh&quot;</code> pour détecter les chapitres (SDD §5.5).</li><li><strong>PDF</strong> : plugin futur via <code>CustomRenderer</code> (le point d&#39;extension existe). Pas urgent.</li></ul><h3 id="_2-5-translation-memory-rag-vol-09" tabindex="-1">2.5 Translation Memory + RAG (Vol 09) <a class="header-anchor" href="#_2-5-translation-memory-rag-vol-09" aria-label="Permalink to &quot;2.5 Translation Memory + RAG (Vol 09)&quot;">​</a></h3><p><strong>Écart technique — TM</strong></p><ul><li>Table <code>translation_memory</code> (<code>001_initial.sql:48-58</code>), index <code>(project_id, source_text)</code>.</li><li><code>TranslationMemoryEngine</code> : <code>exactMatch()</code> (jamais appelé), <code>fuzzyMatches()</code> (Levenshtein sur <strong>toutes les lignes du projet</strong>, threshold 0.85, top 3). <code>TranslateAgent:98</code> n&#39;utilise que fuzzy, sur le 1er paragraphe seulement.</li><li><strong>Pas de segmentation phrase</strong> (SDD §9.2). TM au niveau paragraphe uniquement.</li><li><strong>Pas de normalisation</strong> avant exact match (SDD §9.3).</li><li><strong>Pas de priorité 5 tiers</strong> (project-exact → project-fuzzy → global-exact → global-fuzzy → embeddings) ni de <strong>TM globale cross-projet</strong>.</li><li>TMX import/export OK (<code>fast-xml-parser</code>).</li></ul><p><strong>Écart technique — RAG</strong></p><ul><li><code>RagEngine</code> : embeddings Ollama <code>nomic-embed-text</code> via <code>net.fetch</code>, similarité <code>compute-cosine-similarity</code>, stockage <code>embeddings.embedding_json TEXT</code> JSON.</li><li><code>findSimilar()</code> <strong>brute-force</strong> : charge tous les embeddings du projet, calcule cosinus en JS, top-K sans seuil.</li><li><code>storeEmbedding</code> idempotent par paragraphe (skip si déjà présent) — <strong>pas de réindexation</strong> si changement de modèle.</li><li>1 round-trip Ollama <strong>par paragraphe</strong> pendant la traduction (<code>WorkflowEngine.ts:574-583</code>) — pas de batch.</li></ul><p><strong>Recommandation OSS</strong></p><ul><li>✅ <strong><code>sqlite-vec</code></strong> (cf. §2.1) pour KNN vectoriel — remplace le brute-force O(n) par O(log n).</li><li>✅ <strong><code>minisearch</code></strong> pour préfiltre fuzzy TM two-pass : SQL <code>LIKE &#39;%term%&#39;</code> substring préfiltre → MiniSearch fuzzy <code>{fuzzy:0.2}</code> + Levenshtein refine sur le sous-ensemble. Évite de charger toutes les lignes du projet. <ul><li><code>npm i minisearch</code></li></ul></li><li><strong>Segmentation phrase</strong> : <code>sbd</code> est <strong>déjà en dépendance</strong> et utilisé par <code>ConsistencyChecker</code>. Le réutiliser pour <code>segmentSentences()</code> côté TM.</li><li><strong>Batch embeddings</strong> : <code>OllamaProvider.embeddings()</code> accepter un tableau, appeler <code>/api/embeddings</code> en boucle ou <code>/api/embed</code> (batch natif Ollama récent).</li></ul><h3 id="_2-6-consistency-quality-hallucination-vol-11-12" tabindex="-1">2.6 Consistency + Quality + Hallucination (Vol 11 + 12) <a class="header-anchor" href="#_2-6-consistency-quality-hallucination-vol-11-12" aria-label="Permalink to &quot;2.6 Consistency + Quality + Hallucination (Vol 11 + 12)&quot;">​</a></h3><p><strong>Écart technique — Consistency</strong></p><ul><li><strong>3 métriques sur 7 implémentées</strong> : paragraphs ✅, sentences ✅ (via <code>sbd</code>), length ✅. <strong>Manquent : dialogues, numbers, markup.</strong></li><li><code>named_entities</code> : présence/absence only, pas de comparaison de comptes d&#39;occurrences, pas de gestion des alias.</li><li><strong>Formule de score fausse</strong> : <code>100 - warnings.length*15</code> plat, au lieu de la moyenne pondérée SDD §11.5 (paragraphs:30/sentences:15/dialogues:15/length:10/NE:15/numbers:10/markup:5) + caps (paragraph issue → ≤50, locked-name → ≤70, missing number → ≤80).</li><li><strong>Tolérances trop laxes</strong> : code <code>zh-fr</code> sentence 0.5-2.0 / length 0.6-2.5 vs SDD 0.95-1.05 / 0.5-1.5.</li></ul><p><strong>Écart technique — Quality</strong></p><ul><li><code>QualityChecker</code> explicitement taggué <code>&quot;Version simplifiee sans IA pour le MVP&quot;</code> (<code>:9</code>).</li><li><strong>5/8 dimensions = constantes</strong> : consistency=90, lexicon=90, hallucination=95, dialogue=90, grammar=3 patterns regex triviaux.</li><li><strong><code>QA_SYSTEM_PROMPT</code> jamais appelé</strong>. Pas d&#39;éval LLM pour fluency/style/hallucination/dialogue.</li><li><code>consistency</code> dimension = 90 hardcodé au lieu d&#39;être alimenté par <code>ConsistencyReport.globalScore</code>.</li></ul><p><strong>Écart technique — Hallucination</strong></p><ul><li><code>HallucinationDetector</code> est <strong>construit et testé unitairement</strong> (entités inventées cross-script, références suspectes <code>chapitre N</code>) mais <strong>jamais instancié en production</strong> — seul <code>tests/unit/quality-advanced.spec.ts:339</code> l&#39;utilise. Code mort runtime.</li></ul><p><strong>Recommandation</strong></p><ul><li><strong>Compléter les 7 métriques ConsistencyChecker</strong> : <code>compareDialogues</code> (regex <code>「」&quot;&quot;&#39;&#39;</code> + <code>&quot;-</code>), <code>compareNumbers</code> (<code>/\\d+/g</code> + map occurences), <code>compareMarkup</code> (Markdown <code>**_[]()</code> + tags HTML). ~150 lignes.</li><li><strong>Corriger la formule de score</strong> : moyenne pondérée + caps, strictement SDD §11.5.</li><li><strong>Câbler HallucinationDetector</strong> dans <code>QualityChecker.evaluate()</code> (remplacer le <code>95</code> hardcodé).</li><li><strong>Brancher <code>QA_SYSTEM_PROMPT</code></strong> via LLM eval pour fluency/style/dialogue (dimension <code>hallucination</code> peut rester locale via le détecteur — SDD §12.6 le permet).</li></ul><h3 id="_2-7-securite-vol-21" tabindex="-1">2.7 Sécurité (Vol 21) <a class="header-anchor" href="#_2-7-securite-vol-21" aria-label="Permalink to &quot;2.7 Sécurité (Vol 21)&quot;">​</a></h3><p><strong>Écart technique</strong> (classé par criticité)</p><ol><li>🔴 <strong>CSP production absente</strong> — <code>index.ts:55-56</code> early-return. Pas de meta tag <code>index.html</code>. Violation directe SDD §21.6.</li><li>🔴 <strong>Pas de signature de code</strong> — <code>electron-builder.yml:7,24,25</code> à <code>false</code>. SDD §21.9 mandate Authenticode/Apple. Conséquence : SmartScreen Windows, Gatekeeper macOS, alertes update.</li><li>🟠 <strong>Sel KDF hardcodé</strong> — <code>secrets.ts:20</code> <code>SALT = &quot;NovelTrad-v1-key-derivation&quot;</code>. KDF via <code>scryptSync(userData, SALT, 32)</code> seulement. Un attaquant avec lecture fichier + connaissance du path <code>userData</code> (public) rederive la même clé.</li><li>🟠 <strong>Pas d&#39;OS keyring</strong> — <code>keytar</code>/<code>safeStorage</code> absents (SDD §21.5 tier-1).</li><li>🟠 <strong>Preload log les args IPC</strong> — <code>preload/index.ts:102</code> <code>console.log(&quot;[IPC invoke]&quot;, channel, ...args)</code>. Risque fuite clé API via <code>settings:set</code>/<code>model:test</code> (SDD §21.7).</li><li>🟡 <strong>Gate main-process channels inefficace</strong> — <code>router.ts:41-46</code> <code>ipcMain.on(&quot;message&quot;, preventDefault)</code> inopérant sur les invokes. La vraie protection est côté preload.</li><li>🟡 Heuristique migration plaintext <code>length &gt; 44</code> (clé courte cassée).</li></ol><p><strong>Solide</strong> (à conserver)</p><ul><li><code>webPreferences</code> strict (sandbox+contextIsolation+no nodeIntegration+webSecurity).</li><li>AES-256-GCM + authTag + IV aléatoire.</li><li>Nonce CSRF dédié pour plugin permissions (<code>plugins.ts:40,190-214</code>).</li><li><code>preload-error</code> listener qui surface les échecs preload (défense ajoutée en v2.1.3).</li><li>53 Zod <code>.parse()</code> sur 66 handlers.</li></ul><p><strong>Recommandation</strong></p><ul><li><strong>CSP</strong> : implémenter <code>session.defaultSession.webRequest.onHeadersReceived</code> setant <code>Content-Security-Policy: default-src &#39;self&#39;; script-src &#39;self&#39;; ...</code> <strong>en production aussi</strong> (adapter pour <code>file://</code> : <code>default-src &#39;self&#39; &#39;unsafe-inline&#39; data:</code> minima).</li><li><strong>Signature</strong> : 🚫 WONTFIX (décision produit définitive). La signature de code ne sera jamais activée. Config CSC/Apple retirée de <code>electron-builder.yml</code> et <code>release.yml</code>.</li><li><strong>KDF</strong> : remplacer le sel constant par <code>safeStorage</code> (Electron natif, OS-backed) pour la clé maître + enveler le sel. Si <code>safeStorage</code> indisponible, sel par-machine (<code>machine-id</code>).</li><li><strong>OS keyring</strong> : <code>electron.safeStorage.encryptString()</code> (déjà dans Electron 31, pas de dépendance). Remplace keytar.</li><li><strong>Preload log</strong> : retirer/guarder derrière <code>process.env.NODE_ENV === &quot;development&quot;</code>.</li></ul><hr><h2 id="_3-plan-d-action-immediat-quick-wins" tabindex="-1">3. Plan d&#39;Action Immédiat (Quick Wins) <a class="header-anchor" href="#_3-plan-d-action-immediat-quick-wins" aria-label="Permalink to &quot;3. Plan d&#39;Action Immédiat (Quick Wins)&quot;">​</a></h2><blockquote><p>Objectif : lancer la transition <strong>sans casser les 805 tests</strong> ni le build. Séquencé par risque décroissant et valeur montante.</p></blockquote><h3 id="quick-win-1-—-securite-critique-1-apres-midi-0-rupture" tabindex="-1">Quick Win 1 — Sécurité critique (1 après-midi, 0 rupture) <a class="header-anchor" href="#quick-win-1-—-securite-critique-1-apres-midi-0-rupture" aria-label="Permalink to &quot;Quick Win 1 — Sécurité critique (1 après-midi, 0 rupture)&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 1a. Désactiver le log IPC payload en production (preload)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#     apps/desktop/src/preload/index.ts:102 — wrapper dans if (import.meta.env.DEV)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 1b. Activer CSP production</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#     apps/desktop/src/main/index.ts:54-91 — retirer l&#39;early-return, porter la CSP</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#     dans session.defaultSession.webRequest.onHeadersReceived (SDD §21.6).</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 1c. Brancher safeStorage pour la clé maître (remplace sel hardcodé)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#     apps/desktop/src/main/utils/secrets.ts — electron.safeStorage.encryptString()</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#     avec fallback scrypt + machine-id (pas de sel constant).</span></span></code></pre></div><p><strong>Justification</strong> : 3 correctifs localisés, 0 dépendance, filet de tests déjà présent (<code>secrets.spec.ts</code>, <code>path-traversal.spec.ts</code>).</p><h3 id="quick-win-2-—-migration-runner-unifie-1-journee" tabindex="-1">Quick Win 2 — Migration runner unifié (1 journée) <a class="header-anchor" href="#quick-win-2-—-migration-runner-unifie-1-journee" aria-label="Permalink to &quot;Quick Win 2 — Migration runner unifié (1 journée)&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 2a. Créer le fichier manquant</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apps/desktop/src/main/db/migrations/009_chapter_metadata.sql</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;SQL&#39;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ALTER TABLE chapters ADD COLUMN metadata TEXT;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">SQL</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 2b. Supprimer le tableau inline MIGRATIONS de db/connection.ts:8-362</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#     (ne conserver que le runner par fichiers .sql, connection.ts:408-429).</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 2c. Lancer les tests DB + vérifier qu&#39;une DB fraîche a bien chapters.metadata</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --workspace=apps/desktop</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> db</span></span></code></pre></div><p><strong>Justification</strong> : élimine la divergence source-de-vérité, rend les <code>.sql</code> consultables = source de schéma. Faible risque (les <code>.sql</code> couvrent déjà v1-8).</p><h3 id="quick-win-3-—-workflow-adaptatif-retry-branching-qa-2-jours" tabindex="-1">Quick Win 3 — Workflow adaptatif (retry + branching QA, 2 jours) <a class="header-anchor" href="#quick-win-3-—-workflow-adaptatif-retry-branching-qa-2-jours" aria-label="Permalink to &quot;Quick Win 3 — Workflow adaptatif (retry + branching QA, 2 jours)&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> p-queue</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> p-retry</span></span></code></pre></div><p>Puis dans <code>apps/desktop/src/main/services/providers/OllamaProvider.ts</code> et <code>services/AiRouter.ts</code> :</p><ul><li>wrapper <code>chat()</code> dans <code>pRetry(fn, {retries:3, factor:2, minTimeout:1000})</code> — SDD §7.10.</li></ul><p>Dans <code>managers/WorkflowEngine.ts</code> :</p><ul><li><code>WorkflowRunner.runStep(&#39;qa&#39;)</code> : lire <code>result.score</code>, si <code>&lt;qualityThreshold</code> → <code>this.pause()</code>, si <code>&lt;threshold+20</code> → retry weakest step (<code>retryStep</code> interne), sinon continuer — SDD §7.1.</li><li><code>WorkflowEngine.start()</code> : vérifier <code>this.runners.size &lt; maxConcurrentJobs</code> avant de créer un runner (queue via <code>PQueue</code>).</li></ul><p><strong>Justification</strong> : 2 libs légères (&lt;10 ko), implémente les 3 plus gros écarts fonctionnels du workflow (retry, branching, concurrency), ~100 lignes de code, tests faciles à mocker (déjà des mocks OllamaProvider).</p><hr><h2 id="_4-roadmap-post-quick-wins-priorisee" tabindex="-1">4. Roadmap post-Quick Wins (priorisée) <a class="header-anchor" href="#_4-roadmap-post-quick-wins-priorisee" aria-label="Permalink to &quot;4. Roadmap post-Quick Wins (priorisée)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Priorité</th><th>Bloc</th><th>Effort</th><th>Dépendance</th></tr></thead><tbody><tr><td>P1</td><td>Câbler les 9 prompts TS restants dans les 6 agents LLM</td><td>3 j</td><td>Aucune — prompts déjà écrits</td></tr><tr><td>P2</td><td>ConsistencyChecker 7/7 métriques + formule pondérée + caps</td><td>2 j</td><td>—</td></tr><tr><td>P3</td><td>HallucinationDetector câblé dans QualityChecker</td><td>1 j</td><td>—</td></tr><tr><td>P4</td><td><code>p-queue</code> + <code>p-retry</code> + auto-resume au démarrage (<code>listActive()</code> in <code>whenReady</code>)</td><td>2 j</td><td>Quick Win 3</td></tr><tr><td>P5</td><td>EPUB export → <code>epub-gen-memory</code></td><td>2 j</td><td>—</td></tr><tr><td>P6</td><td>EPUB/DOCX import : lire spine + Heading 1 styleMap</td><td>2 j</td><td>—</td></tr><tr><td>P7</td><td>TM : segmentation phrase (<code>sbd</code>) + match exact câblé + priorité 5 tiers</td><td>3 j</td><td>—</td></tr><tr><td>P8</td><td>RAG : <code>sqlite-vec</code> KNN + seuil + batch embeddings</td><td>3 j</td><td>POC binding wasm</td></tr><tr><td>P9</td><td>TM fuzzy two-pass : <code>minisearch</code> préfiltre</td><td>1 j</td><td>P7</td></tr><tr><td>P10</td><td><code>PromptLoader</code> DB + fallback TS constant</td><td>2 j</td><td>—</td></tr><tr><td>P11</td><td>Signature code (Authenticode + Apple notarize)</td><td>1 j</td><td>Certificat</td></tr><tr><td>P12</td><td>Agent I/O Zod schemas + <code>validateOutput()</code> runner</td><td>2 j</td><td>—</td></tr><tr><td>P13</td><td>Worker threads : fix <code>module.default</code> → import nommé</td><td>0,5 j</td><td>—</td></tr></tbody></table><p><strong>Total estimé</strong> : ~25 jours-homme pour alignement complet SDD. Les Quick Wins (1+2+3) livrent ~40 % de la valeur perçue en ~3 jours.</p><hr><h2 id="_5-notes-methodologiques" tabindex="-1">5. Notes méthodologiques <a class="header-anchor" href="#_5-notes-methodologiques" aria-label="Permalink to &quot;5. Notes méthodologiques&quot;">​</a></h2><ul><li><strong>Le <code>WORKFLOW_STATE.md</code> est optimiste</strong> : il conclut « READY TO DEPLOY, tous volumes couverts » mais l&#39;audit révèle que 6 agents sont heuristic-only, le QA branching est absent, la TM n&#39;est pas segmentée, Consistency n&#39;a que 4/7 métriques. Le revendu &quot;couvert&quot; s&#39;entendait au sens <em>présence de fichiers</em>, pas <em>conformité fonctionnelle au SDD</em>. <strong>Recommandation : introduire un critère &quot;SDD-compliant&quot; (pas seulement &quot;SDD-covered&quot;) dans la Definition of Done.</strong></li><li><strong>Pas de réécriture from-scratch justifiée</strong>. La base saine (Electron + IPC + DB + providers + plugins + UI + tests) représente des milliers de lignes éprouvées. Les écarts sont des <em>trous fonctionnels dans des modules existants</em>, comblables par câblage + ajouts ciblés + 2-3 libs légères.</li><li><strong>Libs à éviter</strong> : <code>better-sqlite3</code> (rebuild natif), <code>bullmq</code> (Redis), toute dépendance Python.</li><li><strong>Libs à adopter</strong> : <code>sqlite-vec</code>, <code>p-queue</code>, <code>p-retry</code>, <code>epub-gen-memory</code>, <code>minisearch</code>. Total : 5 deps, toutes pure-JS, toutes alignées philosophie Node/Ollama local.</li></ul><hr><p><em>Audit réalisé par ZCode (GLM-5.2) le 2026-07-05. Sources : exploration directe du code (13 sous-agents), SDD VitePress <code>docs/volumes/</code>, validation OSS via context7.</em></p>`,95)])])}const m=t(n,[["render",d]]);export{g as __pageData,m as default};
